# Anx Reader APK 发布

这个仓库只用于托管 Anx Reader 的 Android APK 和在线更新清单，不包含应用源代码。

- 更新清单：`update.json`
- APK：GitHub Releases 中的 `anx-reader-*.apk`

发布新版本时，请使用同一份 release 签名证书，并同步更新 `update.json` 中的版本号、`versionCode`、Release APK 地址和 SHA-256。
